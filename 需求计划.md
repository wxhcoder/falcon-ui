# 计划：Falcon UI Monorepo（Element Plus 二次封装）

## 总结
- 搭建 `pnpm` + `changesets` 的 Monorepo，包含 `components`、`theme`、`icons`、
  `hooks`、`utils` 包。
- 在 `packages/` 下新增 `falcon-ui` 作为聚合入口包，用于统一组件引入与按需引入。
- 使用 Vite 8（Rolldown）+ Vue 3 + TypeScript 构建，输出 `ESM + CJS + UMD`。
- Element Plus 作为 `peerDependency`，采用 thin wrapper API；样式使用
  `SCSS + CSS vars`。
- 样式命名规范使用 BEM。
- 集成 `oxlint + oxfmt`（默认规则集）作为唯一 lint/format。
- 集成测试：`Vitest + Vue Test Utils`、`jsdom`、覆盖率开启。
- 增加 `play/` 目录用于临时预览组件，根脚本直接启动。
- 保留 `scripts/` 目录，用于后续编写打包/发布辅助脚本。

## 1) 仓库结构与工作区
- 顶层目录：
  - `packages/`
  - `play/`（独立应用）
  - `docs/`（预留）
  - `scripts/`（构建/发布辅助脚本）
- 包目录：
  - `packages/falcon-ui/`（对外入口包，聚合组件与按需引入）
  - `packages/components/`
  - `packages/theme/`
  - `packages/icons/`
  - `packages/hooks/`
  - `packages/utils/`
- 每个包：
  - `package.json`
  - `src/`
  - `tsconfig.json` 继承根 `tsconfig.base.json`

## 2) 工具链与配置
- 根文件：
  - `pnpm-workspace.yaml`
  - `package.json`
  - `tsconfig.base.json`
  - `.changeset/`
- 根脚本：
  - `build` / `typecheck`
  - `lint` / `format` / `format:check`
  - `test` / `test:watch` / `test:coverage`
  - `changeset` / `version` / `release`
  - `play` / `play:build`

## 3) 构建方案（Vite 8 + Rolldown）
- 每包 `vite.config.ts`（库模式）：
  - 输出：`es` / `cjs` / `umd`
  - `entry: src/index.ts`
  - `external: vue, element-plus`
  - UMD globals: `Vue`, `ElementPlus`
- 类型声明：
  - `vue-tsc --declaration --emitDeclarationOnly`
  - 输出 `dist/types`
- 样式：
  - `theme` 输出 SCSS + CSS vars
  - `components` 引用 `@falcon-ui/theme`
- 打包聚合入口：
  - 以 `packages/falcon-ui` 为统一入口包
  - 通过 `exports` 字段与入口索引实现按需引入

## 4) 包设计
- `@falcon-ui/falcon-ui`（入口聚合包）
  - 聚合导出 `components`、`hooks`、`icons`、`theme`
  - 统一对外 API，支持按需导入
  - 可在此放置打包编排脚本与入口生成逻辑
- `@falcon-ui/components`
  - `install(app)` + 具名导出
  - thin wrapper，保持 EP API
  - `<script setup lang="ts">`
- `@falcon-ui/theme`
  - `index.scss` 入口
- `@falcon-ui/icons`
  - tree-shakeable 具名导出
- `@falcon-ui/hooks`
  - Composition API hooks
- `@falcon-ui/utils`
  - 纯 TS utils

## 5) DX（自动导入）
- `unplugin-vue-components`
- `unplugin-auto-import`
- Volar 类型提示 `.d.ts`

## 6) 测试环境
- `vitest` + `@vue/test-utils`
- `jsdom` + 覆盖率
- `tests/` 对齐 `packages/`

## 7) 代码检查与格式化
- `oxlint` / `oxfmt`
- 默认规则集
- `lint` / `format` / `format:check` 脚本

## 8) Play 临时调试应用
- `play/` 为独立 Vite Vue3 App
- 依赖 workspace 包
- 根脚本启动

## 9) 发布与版本管理
- `changesets`
- 暂不配置 registry
- Element Plus 为 peerDependency

## 样式规范（BEM）
- 组件样式命名统一遵循 `block__element--modifier` 规则。
- 统一前缀（例如 `f-`）用于 block，避免与 Element Plus 冲突。
- 通过 `theme` 包集中维护 BEM 的变量与 mixin。

## 公共 API/接口
- `@falcon-ui/falcon-ui`: 对外入口聚合包
- `@falcon-ui/components`: `install` + 具名导出
- `@falcon-ui/theme`: `index.css`/`index.scss`
- `@falcon-ui/hooks`: hooks 导出
- `@falcon-ui/utils`: utils 导出
- `@falcon-ui/icons`: icon 导出

## 测试场景
- 组件渲染/交互
- theme 变量注入
- 产物完整性（es/cjs/umd）

## 假设与默认
- Node: 最新 LTS
- Vue: 最新稳定版
- Element Plus: 最新稳定版
- 不做 SSR
- 不建文档站
