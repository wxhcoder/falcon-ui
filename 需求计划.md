# 计划：Falcon UI Monorepo（Element Plus 二次封装）

## 总结

- 使用 `pnpm` + `changesets` 搭建 Monorepo。
- `packages/` 下包含 `components`、`theme`、`icons`、`hooks`、`utils`。
- 新增 `packages/falcon-ui` 作为对外入口包，实现统一导入与按需引入。
- 技术栈：Vite 8（Rolldown）+ Vue 3 + TypeScript。
- 样式使用 `SCSS + CSS vars`，命名规范采用 BEM。
- 代码检查与格式化使用 `ESLint + Prettier`。
- 测试使用 `Vitest + Vue Test Utils`，`jsdom` 环境，覆盖率开启。
- 增加 `play/` 目录用于临时预览组件。
- 保留 `scripts/` 目录用于后续构建脚本。

## 1) 仓库结构与入口规范

- 根目录包含 `packages/`、`play/`、`docs/`、`scripts/`、`.npmrc`。
- 包目录包含 `packages/falcon-ui/`、`packages/components/`、
  `packages/theme/`、`packages/icons/`、`packages/hooks/`、
  `packages/utils/`。
- 所有包的入口文件统一放在包根：
  - `index.ts` 位于包根目录。
  - `theme` 的 `index.scss` 位于包根目录。
- `src/` 仅作为内部源码目录，不作为入口文件路径。
- 不再为每个包创建 `tsconfig.json`，统一使用根 `tsconfig.base.json`。

## 2) 包管理与 pnpm 配置

- 根目录新增 `.npmrc` 管理 pnpm：
  - `node-linker=hoisted`（启用变量提升）
  - `strict-peer-dependencies=false`（关闭严格模式）
  - `shell-emulator=true`（启用 shell 模拟器）
  - `link-workspace-packages=true`（工作区包链接到 root node_modules）
- 所有模块依赖统一落在根 `node_modules`。
- 根 `package.json` 通过 `dependencies` 引用各工作区包：
  - `@falcon-ui/components`
  - `@falcon-ui/falcon-ui`
  - `@falcon-ui/hooks`
  - `@falcon-ui/icons`
  - `@falcon-ui/theme`
  - `@falcon-ui/utils`

## 3) 工具链与基础配置

- 根文件包含：
  - `pnpm-workspace.yaml`
  - `package.json`
  - `tsconfig.base.json`
  - `.changeset/`
- 根脚本包含：
  - `build` / `typecheck`
  - `lint` / `format` / `format:check`
  - `test` / `test:watch` / `test:coverage`
  - `changeset` / `version` / `release`
  - `play` / `play:build`

## 4) 构建与打包策略（计划）

- 使用 Vite 8（Rolldown）构建库包。
- `packages/falcon-ui` 作为聚合入口：
  - 统一对外导出组件与工具。
  - 负责按需引入策略的入口组织与导出规范。
- 具体构建产物目标：`ESM + CJS + UMD`。

## 5) 包设计

- `@falcon-ui/falcon-ui`：对外入口聚合包，统一组件导入与按需能力。
- `@falcon-ui/components`：组件封装与导出。
- `@falcon-ui/theme`：主题样式与 CSS vars。
- `@falcon-ui/icons`：图标组件导出。
- `@falcon-ui/hooks`：Composition API hooks。
- `@falcon-ui/utils`：通用工具函数。

## 6) 样式规范（BEM）

- 统一遵循 `block__element--modifier`。
- 使用统一前缀（如 `f-`）避免与 Element Plus 冲突。

## 7) 代码检查与格式化

- `ESLint` 负责 lint，采用 Flat Config（`eslint.config.mjs`）。
- TypeScript 规则使用 `typescript-eslint`，基于 `tseslint.configs.recommended`。
- Vue SFC 校验使用 `eslint-plugin-vue` + `vue-eslint-parser`。
- `Prettier` 负责格式化，`prettier.config.mjs` 作为统一格式化配置。

## 8) 测试环境

- 测试工具：`vitest` + `@vue/test-utils`。
- 运行环境：`jsdom`。
- 覆盖率：开启。

## 9) Play 临时调试应用

- `play/` 为独立 Vite Vue3 应用。
- 依赖 workspace 包，用于快速预览与调试。

## 10) 发布与版本管理

- 使用 `changesets` 进行版本管理。
- 暂不配置 registry。

## 公共 API/接口

- `@falcon-ui/falcon-ui`：统一入口聚合包。
- `@falcon-ui/components`：组件具名导出。
- `@falcon-ui/theme`：`index.scss` / `index.css`。
- `@falcon-ui/hooks`：hooks 导出。
- `@falcon-ui/utils`：utils 导出。
- `@falcon-ui/icons`：icons 导出。

## 测试场景

- 组件渲染与交互。
- 主题变量注入。
- 构建产物完整性（`es/cjs/umd`）。

## 假设与默认

- Node：最新 LTS。
- Vue：最新稳定版。
- Element Plus：最新稳定版。
- 初期不做 SSR。
- 初期不做文档站。

## 11) 新增需求：主题定制与提交门禁（阶段完成）

### 11.1 二次封装组件样式集成 Element Plus 默认样式（试点完成）

- 目标：
  - 二次封装组件的 `scss` 先集成 Element Plus 默认样式，再追加 Falcon 特有样式。
  - 支持 Element Plus 主题配置能力。
- 试点范围：
  - `button`、`input` 两个组件先落地，后续验收通过再固化为全组件模板规范。
- 实施要求：
  - 样式遵循 BEM 规范。
  - 在组件样式中保留 Falcon 扩展区，允许按需增加特有样式。
  - 支持在 BEM block 中注入 EP 组件变量（如 `set-component-css-var`）。
- 本阶段已完成：
  - 新增 `packages/theme/src/ep-vars.scss` 作为 EP 变量桥接入口。
  - `packages/theme/index.scss` 接入 EP 基础样式链路。
  - `packages/theme/src/button.scss` 与 `packages/theme/src/input.scss` 按“EP 基线 + Falcon 扩展”改造。
  - `play/src/views/playground-view.vue` 新增主题定制面板，支持主色与圆角实时预览。
- 完成状态：`已完成（试点）`。

### 11.2 提交时执行单元测试、代码校验、代码格式化（已完成）

- 目标：
  - 每次代码提交前自动执行质量门禁。
- 实施要求：
  - 触发点：`pre-commit`。
  - 必跑项：单元测试、代码校验、代码格式化。
  - 格式化方式：自动修复并纳入当前提交。
- 本阶段已完成：
  - 引入 `husky` 与 `lint-staged`。
  - 新增 `.husky/pre-commit`，执行 `precommit:format` 与 `precommit:quality`。
  - 根脚本新增 `precommit:format`、`precommit:quality`，并统一扩展格式化文件类型。
- 完成状态：`已完成`。

## 进度对齐（与《任务完成记录》比对）

### 总进度

- 已完成大项：`12 / 13`
- 部分完成或待完成：`1 / 13`

### 章节进度打勾

- [x] 1. 仓库结构与入口规范
- [x] 2. 包管理与 pnpm 配置
- [x] 3. 工具链与基础配置
- [x] 4. 构建与打包策略（已完成：已具备 Gulp + Rolldown 打包链路，并产出 `ESM + CJS + UMD`）
- [x] 5. 包设计
- [x] 6. 样式规范（BEM）
- [x] 7. 代码检查与格式化
- [x] 8. 测试环境
- [x] 9. Play 临时调试应用
- [x] 10. 发布与版本管理（按“暂不配置 registry”执行）
- [x] 11. 新增需求：主题定制与提交门禁（阶段完成）
- [x] 公共 API/接口（已完成：`@falcon-ui/theme` 已交付 `index.scss` 与 `index.css`）
- [ ] 测试场景（部分完成：组件渲染与交互已覆盖；按自动化严格口径，主题变量注入与构建产物完整性场景仍未完成）

### 本轮新增完成点（补充）

- [x] 组件按需加载基础设施（`withInstall`、组件可单独 `app.use`）
- [x] 总入口安装逻辑收敛到 `@falcon-ui/falcon-ui`
- [x] play 通过 `app.use(FalconUI)` 使用组件
- [x] 全局注册场景类型提示恢复（`@falcon-ui/falcon-ui/global`）
- [x] README 增加第三方项目类型提示配置说明
- [x] 主题定制试点完成（play 支持主色与圆角实时切换）
- [x] 提交门禁完成（`pre-commit` 执行格式化 + lint + 测试）
