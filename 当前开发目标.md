# 本期开发目标：Button + Input 实装（开发可用）

## 1. 阶段目标
- 在已完成的 `play` 演示壳基础上，实现首批组件 `FButton` 与 `FInput`。
- 交付等级为“开发可用”，不做发布候选级完善。
- 将 `play/src/views/components-view.vue` 从占位展示替换为真实组件示例。

## 2. 已确定实现规则
- `packages/components` 下不使用统一 `src` 目录。
- 每个组件按类型名建一级目录，如 `button`、`input`。
- 每个组件目录结构固定为：
  - `packages/components/<component>/src/<component>.vue`
  - `packages/components/<component>/src/<component>.ts`
  - `packages/components/<component>/index.ts`
- 每个组件的同名 `.ts` 文件必须定义并导出 `props`、`emits` 及类型。
- `.vue` 文件负责模板与组装逻辑，契约定义集中在同名 `.ts`。

## 3. 范围定义
### In Scope
- 实现 `FButton`：`type` / `size` / `disabled` / `loading` + `click` 事件。
- 实现 `FInput`：`v-model` / `placeholder` / `disabled` / `clearable` + `input/change` 事件。
- 新增最小主题变量与 BEM 样式（button/input）。
- 更新组件导出与聚合导出（`components` 与 `falcon-ui`）。
- 替换 `play/components` 页为真实示例。
- 每个组件新增 1-2 条测试用例。

### Out of Scope
- 不做 Element Plus 透传封装规范。
- `Playground` 页面继续保留占位。
- 不补全发布链路（registry/publish/产物策略）。
- 不实现高级扩展能力（icon/复杂 slot 等）。

## 4. 实施任务拆解
### 4.1 组件实现
- `packages/components/button/src/button.ts`
- `packages/components/button/src/button.vue`
- `packages/components/button/index.ts`
- `packages/components/input/src/input.ts`
- `packages/components/input/src/input.vue`
- `packages/components/input/index.ts`

### 4.2 样式与主题
- `packages/theme/src/tokens.scss`
- `packages/theme/src/button.scss`
- `packages/theme/src/input.scss`
- `packages/theme/index.scss` 汇总引入

### 4.3 导出与接入
- `packages/components/index.ts` 导出 `FButton`、`FInput`
- `packages/falcon-ui/index.ts` 保持聚合导出可用
- `play/src/views/components-view.vue` 替换为真实组件示例

### 4.4 测试与质量
- `packages/components/button/src/button.test.ts`
- `packages/components/input/src/input.test.ts`
- 运行并通过：
  - `pnpm lint`
  - `pnpm format:check`
  - `pnpm test`
  - `pnpm play:build`

## 5. 验收标准（开发可用）
- `FButton` 基础四项能力可用，`disabled/loading` 时阻断 click。
- `FInput` 基础五项能力可用，clearable 可清空并同步更新值。
- `play/components` 可直接演示 Button/Input 的核心交互。
- 以上质量命令全部通过。

## 6. 风险与处理
- 风险：`eslint@latest` 与部分插件存在 peer warning。
  - 处理：以命令可运行和验证通过为准；出现真实兼容问题再切兼容版本组合。
- 风险：组件能力范围扩张导致周期失控。
  - 处理：严格限制在本目标定义的基础能力，不额外加需求。
