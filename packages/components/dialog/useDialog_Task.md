# useDialog Task Checklist

## 任务目标

基于 [UseDialog_RPD.md](./UseDialog_RPD.md) 完成 `useDialog` 调用式弹窗服务开发，
并补齐导出、测试与文档示例。

## 执行清单

- [x] T1 新建任务清单文档并初始化执行项
- [x] T2 新增 `useDialog` 核心类型与服务实现（`open` / `closeAll`）
- [x] T3 完成 hooks 导出链路接入（对外可导入使用）
- [x] T4 补齐 `useDialog` 单元测试（核心流程与边界）
- [x] T5 增加 Dialog 文档中的 `useDialog` 用法与示例
- [x] T6 运行并通过本次改动相关测试

## 备注

- 本期不改造 `FlInputSearch` 内部逻辑，仅保持兼容与迁移说明。
- 每完成一个任务，实时更新本清单勾选状态。
