这是一份为你整理的完整、标准化的**组件库文档系统开发指南**。你可以直接将其保存为 DOCS\_SYSTEM\_DESIGN.md，作为你组件库文档模块的指导性技术文档。

# ---

**组件库文档系统 (Documentation System) 开发设计规范**

本规范参考 **Element Plus** 的工程化实践，详细描述基于 **VitePress** 构建高性能、自动化组件文档系统的核心逻辑与实现方案。

## ---

**1\. 核心技术栈**

* **SSG 引擎**: (基于 Vite \+ Vue 3\)  
* **Markdown 解析**: markdown-it  
* **代码高亮**: shiki (支持 VS Code 级别的 Token 着色)  
* **元数据提取**: vue-component-meta 或 ts-morph  
* **在线调试**: @vue/repl

## ---

**2\. 目录架构规范**

推荐采用 **“源码与示例分离”** 的架构，确保示例代码可被 IDE 校验、可被单元测试覆盖。

## ---

**3\. 核心机制：Markdown 插件系统 (::: demo)**

这是文档系统最关键的“魔法”，负责将 Markdown 中的标记转化为可运行的交互卡片。

### **3.1 解析流程**

1. **拦截容器**：利用 markdown-it-container 识别 ::: demo \[path\] 语法。  
2. **源码抓取**：插件根据路径从 docs/examples/ 读取真实的 .vue 源码。  
3. **高亮处理**：使用 shiki 将源码转为 HTML 高亮字符串。  
4. **组件封装**：将高亮后的 HTML 和原始代码封装进自定义 Vue 组件（如 \<vp-demo\>）。  
5. **动态注入**：在渲染阶段，插件会自动在当前 MD 文件的 \<script setup\> 中生成该示例组件的 import 语句。

## ---

**4\. 自动化 API 生成规范**

为了降低维护成本，严禁手动编写 API 表格，需采用 **“代码即文档”** 策略。

* **元数据提取**：通过脚本扫描组件的 props、slots、events。  
* **注释规范**：  
* **表格转化**：利用脚本将提取出的 JSON 元数据自动填充到 Markdown 文件的指定位置。

## ---

**5\. 样式与主题适配**

* **CSS 变量联动**：组件库的主题色（如 \--el-color-primary）应与 VitePress 的品牌色（如 \--vp-c-brand）保持映射。  
* **Dark Mode**：监听 VitePress 的 .dark 类名切换，确保组件在深色模式下的 UI 表现正常。

## ---

**6\. 实施路线图 (Roadmap)**

1. **阶段一**：搭建 VitePress 基础架构，配置多语言路由。  
2. **阶段二**：实现 docs/.vitepress/plugins 逻辑，打通 ::: demo 渲染链路。  
3. **阶段三**：开发自定义 vp-demo 组件，支持代码展开、复制、外部跳转。  
4. **阶段四**：接入 vue-component-meta 实现 API 自动同步。  
5. **阶段五**：部署全量构建版本，集成 @vue/repl 实现在线演练场。

### 当前开发进度标记（Falcon UI）

- [x] 阶段一：搭建 VitePress 基础架构（本期按“中文单语”执行）。
- [x] 阶段二：实现 docs/.vitepress/plugins 逻辑，打通 ::: demo 渲染链路。
- [x] 阶段三：开发自定义 vp-demo 组件，支持代码展开、复制、外部跳转。
- [x] 阶段四：接入 vue-component-meta 实现 API 自动同步。
- [x] 阶段五：完成全量构建验证（本期按既定范围未集成 @vue/repl，预留二期）。

**7.布局描述**

### **1\. 顶部导航栏 (Global Header)**

这是用户切换大模块（如指南、组件、资源）的入口。

* **左侧**：Logo 及版本切换器（Version Select）。  
* **右侧**：组件导航、搜索框（通常集成 Algolia）、深色模式切换、语言切换及 GitHub 链接。  
* **开发要点**：使用 `position: sticky` 固定在顶部。需实现移动端下的“汉堡菜单”切换。

### **2\. 左侧侧边栏 (Component Sidebar)**

负责展示组件的索引。

* **结构**：展示组件列表。  
* **逻辑**：  
  * **自动激活**：根据当前 URL 路由自动高亮对应的菜单项。  
  * **滚动同步**：当内容区滚动时，侧边栏保持不动；若菜单过长，侧边栏应具备独立的滚动条。  
* **开发要点**：使用 `Vue Router` 的 `linkActiveClass` 实现自动高亮，配合 `SimpleBar` 等插件优化滚动条样式。

### **3\. 右侧大纲栏 (Anchor Table of Contents)**

这是针对单个组件页面的内部分析。

* **功能**：展示当前页面的 H2、H3 标题（如“基础用法”、“Button Attributes”）。  
* **交互**：点击平滑滚动到对应位置；滚动页面时，右侧锚点随之切换激活状态。  
* **开发要点**：利用 `Intersection Observer API` 监听内容区的标题可见性，从而更新锚点的激活状态。

### **4\. 中央内容区 (Main Content)**

这是最复杂的模块，由 Markdown 转换而成。

* **组件示例 (Demo Block)**：  
  * **预览区**：实际运行的组件效果。  
  * **工具栏**：包含“复制代码”、“查看源代码”等功能按钮。  
  * **源码区**：使用 `shiki` 或 `prismjs` 渲染的带语法高亮的代码块，支持折叠动画。  
* **API 表格**：清晰展示属性、事件、插槽。  
* **开发要点**：这是之前提到的 `markdown-it` 插件的核心产出物。
